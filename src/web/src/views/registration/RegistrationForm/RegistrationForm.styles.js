import styled from 'styled-components'

import * as Login from '@/views/login/LoginForm/LoginForm.styles'

export const Wrapper = styled(Login.Wrapper)``

export const Error = styled(Login.Error)``

export const Title = styled(Login.Title)``

export const Form = styled(Login.Form)``

export const FormRow = styled(Login.FormRow)``

export const Input = styled(Login.Input)``

export const InputError = styled(Login.InputError)``

export const Btn = styled(Login.Btn)``

export const Toolbar = styled(Login.Toolbar)``

export const RouterLink = styled(Login.RouterLink)``
